Description: set dirs and properly clean

--- sagecal-0.1.orig/src/MS/Makefile
+++ sagecal-0.1/src/MS/Makefile
@@ -1,15 +1,15 @@
 OUTPUT=
 CXX=g++
 CXXFLAGS=-O3 -Wall -g #-fnostack-protector
-CASA_LIBDIR=/opt/casacore/lib
-CASA_INCDIR=/opt/casacore/include/casacore
+CASA_LIBDIR=/usr/lib
+CASA_INCDIR=/usr/include/casacore
 CASA_LIBS=-lcasa_casa -lcasa_tables -lcasa_measures -lcasa_ms -lcfitsio
 #LAPACK=-llapack -lblas
-LAPACK=-lopenblas -lgfortran -lpthread
-LAPACK_DIR=/usr/local/OpenBLAS/lib/
+LAPACK=-lblas -lgfortran -lpthread -llapack
+LAPACK_DIR=/usr/lib/
 #LAPACK_DIR=/usr/lib/atlas/sse/
 
-LDFLAGS=-Wl,--rpath,/usr/local/OpenBLAS/lib/
+LDFLAGS=-Wl,--rpath,/usr/lib/
 #LDFLAGS=-Wl,-t,--rpath,/software/users/lofareor/SW/lib64
 # -Wl,--hash-style=both
 
@@ -32,4 +32,4 @@ data.o:data.cpp data.h
 sagecal:$(OBJECTS) ./lib/libsagecal.a
 	$(CXX) $(CXXFLAGS) $(LDFLAGS) $(INCLUDES) $(GLIBI) $(LIBPATH)  -o $@  $(OBJECTS) $(MY_LIBS) $(LAPACK) $(CASA_LIBS)  $(GLIBL)
 clean:
-	rm *.o *.tmp *.fits
+	rm -f *.o *.tmp *.fits sagecal
--- sagecal-0.1.orig/src/lib/Makefile
+++ sagecal-0.1/src/lib/Makefile
@@ -50,3 +50,6 @@ RANLIB=ranlib
 libsagecal.a:$(OBJECTS) sagecal.h
 	ar rv $@ $(OBJECTS); \
 	$(RANLIB) $@;
+
+clean:
+	rm -f *.o *.tmp *.a
